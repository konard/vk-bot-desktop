---
'vk-bot-desktop': patch
---

Document macOS Gatekeeper first-launch bypass for ad-hoc signed releases in the README and the GitHub Pages download page (English and Russian). Covers the Terminal `xattr -dr com.apple.quarantine` workflow and the macOS 15 Sequoia System Settings → Privacy & Security → Open Anyway workflow.
