---
'vk-bot-desktop': patch
---

Fix Windows ARM64 release smoke testing so GitHub-hosted ARM runners validate generated artifacts without launching the NSIS installer path that exits with an access violation.
